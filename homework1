def mangdaduocsapxep(a):
  for i in range(len(a)):
    x = 0
    int(x)
    for j in range(x,len(a)-1):
      if a[j]>a[j+1]:
        a[j],a[j+1]=a[j+1],a[j]
  return a

a = [9, 3, 2, 15, 1, 4, 6, 5]
print(mangdaduocsapxep(a))
